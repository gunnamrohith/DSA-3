# Source Code

This folder contains all executable project code.

- `src/`: Java similarity engine, models, tokenizer, CLI, and HTTP server
- `web/`: browser dashboard
- `sample_codes/`: pairwise CLI test samples
- `build.sh`: compile and run algorithm self-tests
- `run.sh`: build when required and start the local server

Run the application:

```bash
./run.sh
```